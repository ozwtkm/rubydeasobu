<%
	result = {}

	if !c[:e].nil? 
		result = {:ErrorMessage=>c[:e].message}
	end 

	if !c[:monster].nil?
		result = {
			:id=>c[:monster].id,
			:name=>c[:monster].name, 
			:rarity=>c[:monster].rarity
		}
	end

	if !c[:gachas].nil?
		result = c[:gachas].map do |k,v|
			{
				:id=>k,
				:name=>v
			}
		end
	end

%><%= JSON.generate(result) %>