<%
	result = {}

	if !c[:e].nil? 
		result = {:ErrorMessage=>c[:e].message}
    else 
        c[:dangeons].each do |k,v|
            result[k] = v.name
        end

	end
%><%= JSON.generate(result) %>