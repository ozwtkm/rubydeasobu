<%
	result = {}

	if !c[:e].nil? 
		result = {:ErrorMessage=>c[:e].message}
	end 

	if !c[:map].nil?
		result = c[:monsters].map do |row|
			{:name=>row.name, :rarity=>row.rarity}
		end
	end
%><%= JSON.generate(result) %>