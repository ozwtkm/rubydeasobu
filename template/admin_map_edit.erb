<%
	result = {}

	if !c[:e].nil? 
		result = {:ErrorMessage=>c[:e].message}
	elsif !c[:map].nil?
		result = []
		c[:map].rooms.each do |row|
			result <<row
		end
	else
		result = {:Message=>"つくったお"}
	end
	
%><%= JSON.generate(result) %>